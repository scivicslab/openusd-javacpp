// Included last by the generated JNI translation unit (see the preset's include list).
// OpenUSD declares everything inside a versioned namespace and re-exports it as `pxr`;
// the generated JNI code refers to the classes unqualified, so bring `pxr` into scope.
#pragma once
#include "pxr/pxr.h"
PXR_NAMESPACE_USING_DIRECTIVE
